{
	"hooks": {
		"PreToolUse": [
			{
				"matcher": "Bash",
				"hooks": [
					{
						"type": "command",
						"command": "cmd=$(jq -r '.tool_input.command'); blocked=false; echo \"$cmd\" | grep -qE '(^|[|;&])\\s*(npm\\s+(install|i|ci|add|remove|uninstall|update)|bun\\s+(install|add|remove)|yarn\\s+(install|add|remove))' && blocked=true; if echo \"$cmd\" | grep -qE '(^|[|;&])\\s*pnpm\\s+'; then echo \"$cmd\" | grep -qE '(^|[|;&])\\s*pnpm\\s+(install|add|remove|uninstall|update|up|upgrade|link|unlink|import|fetch|prune|store|audit|outdated|why|pack|publish|root|bin|list|ls|ll|la|info|view|show|config|cache|env|self-update|create|setup|doctor)(\\s|$)' || blocked=true; fi; $blocked && printf '%s' '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"Wrong package manager: pnpm is for deps only (pnpm install/add/remove). Use bun run for scripts: bun run check, bun run type-check, bun run test, bun run lint. Never: npm/yarn install, bun install, or pnpm run.\"}}' || exit 0"
					}
				]
			}
		]
	}
}
